@require '../styles/global.styl'

.section
  position: relative
  z-index: 100

  // display: flex

  .wrapper
    padding: 0 20% 0 20%

  h1
    font-size: 5vw
    font-weight: 700
    text-transform: uppercase
    margin-bottom:8em

  &:first-child
    padding-top: 1em
    padding-bottom: 1em
