@require '../styles/global.styl'

.section
  z-index: 100
  // flex: 50% 0 0

  // display: flex
  // flex-flow: row wrap
  // overflow: hidden

  &.narrow
    padding: 3vw

  +below(screens.tablet)
    padding: 1em

  &.chapter
    position: fixed
    width: 50%
    text-align: right
    padding: 4vw
    height: 100vh

    display: flex
    flex-direction: column
    align-content: flex-end


  &.chapterContent
    position: relative
    width: 50%
    left: 50%
